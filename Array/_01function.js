/*
(), pop(), splice(), concat(), slice(), and forEach(). These methods provide functionality for adding, removing, modifying, or iterating over array elements.*/

const numbers = [1, 2, 3, 4, 5];
console.log(numbers); // Output: [1, 2, 3, 4, 5]

const fruits = ['apple', 'banana', 'orange'];
console.log(fruits[1]); // Output: 'banana'

fruits.push('grape');
console.log(fruits); // Output: ['apple', 'banana', 'orange', 'grape']

fruits.splice(1, 2);
console.log(fruits); // Output: ['apple', 'grape']

//The splice method in JavaScript modifies an array by removing or replacing existing elements and/or adding new elements.




//2. fix the decimal upto 2 decimal places
//=> toFixed(number)
console.log(bmi.toFixed(2) + " " + bmiCategory);


